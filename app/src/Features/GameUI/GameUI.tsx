import { Button, Modal } from "antd";
import { FC } from "react";

const GameUI: FC = () => {
    return (
        <>
            <Modal open>
                <Button>Play</Button>
            </Modal>
        </>
    );
};

export default GameUI;
